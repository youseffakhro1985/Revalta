Redeploy marker for the latest main build.
Triggered 2026-08-19T23:33:44Z by Claude.ai.

Production (www.revalta.se) was confirmed still serving #305 (0dbb58d) while
GitHub main was already at #306 (a8354f3) and beyond -- the GitHub->Vercel
auto-deploy did not fire for a8354f3 (no deployment record at all in Vercel,
not even ERROR/CANCELED). This commit is a content-only nudge to main to
force a fresh push event and a real target:production deployment, the same
pattern used for the 2026-07-15 redeploy. No application code changed.

See AI_COLLABORATION.md ("Verifierad snapshot") and docs/AI_TASKS.yml
(snapshot.production) for the full finding.

PR #381 preview retry — 2026-09-01T21:23:00Z.
The previous exact-SHA candidate cc18f3de54e0425b7a44ee2d640e292a5732128b was
rejected by Vercel with build-rate-limit before Preview Browser E2E could run.
This marker intentionally creates a fresh candidate SHA without changing application
behavior so the normal Vercel Preview + exact-SHA release gates can be retried.

PR #382 preview retry — 2026-09-01T21:43:00Z.
The first Stripe lifecycle candidate a108466b718af62a22972c69095444347ed77d16
passed Revalta CI and CodeQL, but Vercel rejected its Preview with build-rate-limit
before Browser E2E could resolve an exact-SHA deployment. This marker creates one
fresh candidate SHA without changing application behavior so all release gates can
be rerun on the same deployable candidate.
