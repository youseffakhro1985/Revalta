# Tenant security matrix

Source: current code on the branch that introduced this file. This is an evidence map, not a claim that every route is proven.

Negative tests must use two companies (Tenant A / Tenant B). A mocked `findFirst` that already contains `company_id` is necessary but not sufficient for related-object attacks.

## Contract

- Staff reads/writes are scoped by the session company, never by a client-supplied `company_id`.
- Related ids (`property_id`, `ticket_id`, `lease_id`, `document_id`, `vendor_id`, `unit_id`, `component_id`, `project_id`, `work_order_id`) must be re-loaded with `company_id` of the caller. Helper: `findCompanyOwned` in `src/lib/tenant-relations.ts`.
- Cross-tenant misses return **404** (`tenantSafeMissResponse`) rather than 403 that confirms the row exists.
- Residents must not inherit staff company-scope. Resident access is identity/lease/unit scoped.
- Soft-deleted parents must not be mutated through child APIs.
- Public portal uses an explicit portal tenant only. Foreign company UUIDs are not a slug.

## Domain map (conservative)

| Domain | Direct company scope | Related-object scope | Current automated evidence | Residual risk |
| --- | --- | --- | --- | --- |
| Properties / buildings / units | Yes on property APIs | Building/asset checks in work-order asset links | Property route 404 tests; `validateWorkOrderAssetLinks` | Full unit/building matrix incomplete |
| Tickets / comments / attachments | `company_id` on ticket routes | Property and assignee re-read via `findCompanyOwned`; comments/attachments require parent ticket in session company before blob write; GET/PATCH/DELETE look up ticket via `tenantWhere`; WO-from-ticket looks up ticket by session `company_id`; restore looks up deleted ticket by session `company_id`; timeline/operations/SMS/AI look up ticket via `tenantWhere` | Ticket POST Tenant B property/assignee 404; comment/attachment Tenant B 404; blob GET scoped through parent ticket; GET/PATCH/DELETE Tenant B ticket id 404; WO-from-ticket POST Tenant B ticket id 404 without transaction; restore Tenant B ticket id 404 without transaction; timeline GET Tenant B ticket id 404; operations GET/POST/PATCH/DELETE Tenant B ticket id 404; SMS/AI POST Tenant B ticket id 404 | Internal comment body still listed only after parent 404 |
| Resident portal | Must be stricter than staff | Ticket/lease ownership via reporter/lease-holder email; staff APIs 403 for residents | Resident helper email+company tests; `findResidentMatchedLease` by id (not list membership); Resident A cannot create ticket/booking on Tenant B or Resident B lease (404); cancel Tenant B booking 404; notices only `lease_id in` matched; staff `/api/leases` and `/api/bookings` 403 for residents | Golden-path Preview E2E still P1 |
| Work orders / execution / time / material | `company_id` + property join | Property/ticket 404; unit via property.company_id; assignee/vendor related-id 404; recurring POST re-reads property in caller company; PATCH looks up schedule via company-scoped `readRecurringSchedules`; restore looks up deleted WO by session `company_id`; report GET constrains `id` + session `company_id` then `findAccessibleWorkOrder`; comments/time/materials go through `findAccessibleWorkOrder` before list/create; execution/transitions look up WO by session `company_id`; invoice-basis looks up WO by session `company_id`; edit-lock/attestation go through `findAccessibleWorkOrder`; locked-update PATCH looks up WO by session `company_id`; SLA GET/PATCH via `resolveSla`; profitability/invoice-integration/asset-options/project/reports look up WO by session `company_id` | Create tenant atomicity + vendor assignment + mutation atomicity; Preview golden-path planned→invoiced; recurring POST Tenant B propertyId 404 without upsert; PATCH Tenant B schedule id 404 without updateMany; restore Tenant B WO id 404 without transaction; report GET Tenant B id 404 without WO lookup; comments/time/materials GET/POST Tenant B WO id 404 without list/upsert; execution GET/POST and transitions GET Tenant B WO id 404; invoice-basis GET/POST Tenant B WO id 404; edit-lock GET/POST and attestation POST Tenant B WO id 404; locked-update PATCH Tenant B WO id 404 without transaction; SLA GET/PATCH Tenant B WO id 404 without audit/mutation; profitability GET/POST Tenant B WO id 404 without settings upsert; invoice-integration GET/POST Tenant B WO id 404 without export queue; asset-options GET Tenant B WO id 404 without building/asset list; project POST Tenant B WO id 404 without create; reports GET/POST Tenant B WO id 404 without snapshot/create; detail GET Tenant B WO id 404; invoice-basis export GET Tenant B WO id 404; recurring incident POST Tenant B notificationKey 404 without create | Dedicated technician-role E2E still P1 |
| Projects / maintenance / components | Company filters present | Property joins in several engines; project GET by session `company_id`; maintenance POST re-reads property via `tenantWhere`; component writes re-read property via `tenantWhere` and related WO/project/lifecycle ids in caller company; manage looks up asset in caller company; project comments look up project by session `company_id` before list/create; restore looks up deleted project by session `company_id`; maintenance-plan action PATCH looks up property via `tenantWhere`; inspection work-order helper looks up lease by session `company_id` | Project POST Tenant B property/manager/WO 404; project GET Tenant B id 404; maintenance POST Tenant B propertyId 404; PATCH Tenant B item id 404; component PATCH/action/report/entry/manage Tenant B property/asset/project/lifecycle/cost 404; project comments GET/POST Tenant B project id 404 without list/create; restore Tenant B project id 404 without transaction; maintenance-plan action PATCH Tenant B property id 404; `createInspectionWorkOrders` Tenant B lease id 404 without transaction; maintenance-plan action work-order POST Tenant B property/action id 404 without create | Dedicated technician-role Preview fixture still P1 |
| Documents / operational documents | Company filters; parent constraint migration exists | Parent must be same company; download lookup by session `company_id`; list/POST resolve entity in caller company; work-order documents via `findAccessibleWorkOrder`; library filters keep caller `company_id`; lifecycle PATCH looks up document by session `company_id` | Document download Tenant B 404 without blob; operational download/list/upload/delete Tenant B 404; WO GET/POST/download Tenant B 404; library Tenant B propertyId stays in caller company; managed upload Tenant B property/lease 404; PATCH Tenant B document id 404; lifecycle PATCH Tenant B document id 404 without mutation | Remaining quote PDF/output if added |
| Rounds / inspections | Company filters present | Lease/property joins in inspection helpers; POST re-reads property via `tenantWhere`; PATCH/work-order look up inspection by session `company_id`; round POST re-reads property via `tenantWhere`; round PATCH looks up round by session `company_id`; checklist template PATCH/DELETE constrain `id` + session `company_id` | Inspection POST Tenant B propertyId 404; PATCH Tenant B inspection id 404; work-order POST Tenant B inspection id 404 before create; round POST Tenant B propertyId 404 without create; round PATCH Tenant B id 404 without updateMany; checklist PATCH/DELETE Tenant B template id 404 without audit; round work-order POST Tenant B round id 404 without transaction | Production schema of checklist templates unverified |
| Leases / holders / handover / rent notices | Company + property deleted_at | Lease lookup helpers; rent notice create 404s Tenant B `leaseId`; resident staff-lease APIs 403; search lease-holders by session `company_id`; GET holders re-read property in caller company; PATCH/DELETE/restore look up holder by session `company_id`; lease POST re-reads unit via property.company_id and holder in caller company; lease restore looks up by session `company_id`; inspection items GET/PUT look up lease by session `company_id` | Rent notice POST Tenant B lease; resident GET/PATCH/DELETE `/api/leases` 403; handover GET/PUT Tenant B lease id 404 before payload/mutation; search Tenant B lease-holder query stays in caller company; GET holders Tenant B `propertyId` 404 without findMany; PATCH/DELETE Tenant B holder id 404 without updateMany; restore Tenant B holder/lease id 404 without transaction; POST Tenant B `unitId`/`holderId` 404; inspection-items GET/PUT Tenant B lease id 404; inspection-items work-orders GET/POST Tenant B lease id 404; reconcile Tenant B lease id 404; status GET keeps Tenant B lease id in caller company | Remaining dedicated lease-holder export API if added |
| Bookings / access credentials | Company filters present | Resident create uses `findResidentMatchedLease`; list/cancel by `created_by_id` + `company_id`; credential POST re-reads property via `tenantWhere`; PATCH looks up credential by session `company_id`; staff POST re-reads property via `tenantWhere`; staff PATCH looks up booking by session `company_id` | Resident A vs Tenant B/Resident B lease 404; cancel Tenant B booking 404; staff `/api/bookings` 403 for residents; access-credential POST Tenant B propertyId 404; PATCH Tenant B credential id 404; staff POST Tenant B propertyId 404; staff PATCH Tenant B booking id 404 | Remaining booking conflict/concurrency Preview |
| Quotes / vendors / insurance | Company filters present | Vendor create property via `findCompanyOwned`; quote/claim POST re-reads property via `tenantWhere`; PATCH/work-order look up quote/claim by session `company_id` | Vendor POST Tenant B propertyId 404; quote POST Tenant B propertyId 404; PATCH Tenant B quote id 404; work-order POST Tenant B quote id 404; insurance-claim POST Tenant B propertyId 404; PATCH Tenant B claim id 404; work-order POST Tenant B claim id 404 | Remaining quote PDF/output if a dedicated export is added |
| Energy / IMD / budget / calendar | Company filters present | Property and IMD lease re-read in caller company; calendar events by session `company_id`; WO/lease calendar projections by session `company_id` | Energy POST Tenant B propertyId 404; DELETE Tenant B reading id 404; IMD POST Tenant B propertyId 404; IMD POST Tenant B leaseId 404; budget POST Tenant B propertyId 404; DELETE Tenant B entry id 404; calendar PATCH/DELETE Tenant B event id 404; calendar GET lease and work-order company scope | Dedicated technician-role Preview fixture still P1 |
| Notifications / audit / integrations | Company scoped lists | Inbox PATCH/DELETE look up notification by session `company_id`; SLA unknown keys 404; service-center assignee/key 404 | Inbox PATCH Tenant B notification id 404 without upsert; DELETE Tenant B id 404 without updateMany; SLA foreign key 404; service-center foreign assignee/key 404 | Remaining notification export surfaces |
| Team / members | Session `company_id` on user rows | PATCH looks up target by session `company_id` before `updateOwnedByCompany` | PATCH Tenant B member id 404 without updateMany/audit | Remaining invite-accept is intentionally unauthenticated |
| Invoice export / billing | Company on work-order finance routes | Work order ownership; retry/cancel look up job via company-scoped `latestJobs`; GET/POST invoice-integration look up WO by session `company_id`; invoice-basis export looks up WO by session `company_id` | Invoice tenant-isolation tests; POST retry Tenant B job id 404; GET/POST Tenant B WO id 404 before draft/job reads; GET invoice-basis export Tenant B WO id 404 without draft/audit | Provider readiness still P1 |
| Search / export | Company where clauses | CSV cells quoted; ticket, maintenance-plan, audit and SLA-report exports prefix `=`/`+`/`-`/`@`; client dashboard exports use shared `csvCell` | Search company scope; ticket export company scope; ticket/maintenance-plan/audit/SLA-report CSV prefixes Tenant B formula titles; maintenance-plan export Tenant B property 404; `csv-cell` tests cover dashboard keys/docs/claims/properties/budget/energy/IMD/quotes/reports | Remaining lease-holder export if a dedicated API is added |
| Public portal | Explicit portal company | Property must belong to that company; foreign property 404 | `public-portal-tenant.test.ts`; public ticket property 404 | Commercial tenant id is owner decision |

## Two-tenant negative cases (minimum)

Tenant A authenticated requests against Tenant B ids must 404/403 per API contract for GET, PATCH, DELETE, related-id attach, export, search, and blob download.

This file is updated when a domain’s tests move from mock-scope proofs to two-organisation proofs. Do not mark a domain READY from this table alone.
