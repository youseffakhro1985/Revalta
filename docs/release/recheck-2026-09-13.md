# Release recheck — 2026-09-13

Purpose: trigger and record a fresh exact-SHA release verification for the active Revalta stabilization path without changing runtime, database schema, business logic, auth behavior, tenant logic, or provider configuration.

## Production baseline

- main SHA before this recheck: `807457b5619ccf66b7ff2b48834245dd4fc8cc60`
- production must remain unchanged until all mandatory release gates are green.

## Required gates

The active release candidate is not mergeable until the exact current head has green evidence for:

- Revalta CI
- CodeQL
- Vercel Preview deployment
- Preview Health Attestation
- Preview Browser E2E
- clean migration verification
- production dependency audit with 0 high/critical vulnerabilities
- no known tenant isolation regression

## Preview data isolation

Preview must never use Production PostgreSQL. `DATABASE_URL` and `DIRECT_URL` for the release candidate must both resolve to the dedicated isolated Preview data plane and `/api/health` must attest the reviewed Preview identity before browser fixtures or mutations are allowed.

This commit is documentation-only and exists to force a fresh exact-SHA verification after GitHub write access was restored on 2026-09-13.
